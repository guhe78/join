/**
 * Initializes the legal/privacy page with navigation and user display.
 * Sets the active navigation link and renders the logged-in user's initials.
 */
function legalPrivacyInit() {
  setActiveLink();
  renderLoginInitials();
}
